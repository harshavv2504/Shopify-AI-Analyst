[pytest]
# Pytest configuration for async tests
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Logging
log_cli = false
log_cli_level = INFO

# Coverage
addopts = 
    --strict-markers
    --tb=short
    -v
